#!/usr/bin/env ruby

require_relative "../lib/sherlock"

require "json"

training_mode = !ARGV.delete("--no-train")

solver = Sherlock::Solver.new(train: training_mode)
solver.add_strategy(Sherlock::Strategies::ThereCanBeOnlyOne)
solver.solve
